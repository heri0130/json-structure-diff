# 의존성 / 빌드 산출물 (저장소에 올리지 않음 — npm install / npm run build 로 재생성)
node_modules
dist
dist-ssr

# TS 빌드 부산물 (vue-tsc -b 가 생성)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# 로컬 환경설정
*.local
.env
.env.*

# 에디터 / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log
